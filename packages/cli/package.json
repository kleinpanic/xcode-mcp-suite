{
  "name": "@kleinpanic/xcmcp",
  "version": "0.1.0",
  "description": "CLI for Xcode's MCP bridge — build, test, preview, REPL and more",
  "license": "MIT",
  "author": "kleinpanic",
  "bin": { "xcmcp": "./dist/cli.js" },
  "main": "./dist/cli.js",
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/cli.ts --format esm --banner.js '#!/usr/bin/env node' --dts --clean",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kleinpanic/xcode-mcp-sdk": "workspace:*"
  },
  "devDependencies": {
    "tsup": "*",
    "typescript": "*",
    "@types/node": "*"
  }
}
